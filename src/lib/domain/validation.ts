import { z } from 'zod'; export const quantity=z.number().int().nonnegative(); export const paise=z.number().int().nonnegative(); export function parseUnambiguousDate(value:string|Date):Date { if(value instanceof Date){if(!Number.isNaN(value.valueOf()))return value;throw new Error('Invalid date.')} if(!/^\d{4}-\d{2}-\d{2}$/.test(value))throw new Error('Use ISO YYYY-MM-DD dates.'); const d=new Date(`${value}T00:00:00.000Z`);if(Number.isNaN(d.valueOf()))throw new Error('Invalid date.');return d; }
