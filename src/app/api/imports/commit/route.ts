import {NextResponse} from 'next/server';import {authenticated} from '@/lib/server/auth';import {commitImport} from '@/lib/server/import-service';import {MongoRepository} from '@/lib/server/repository';export async function POST(req:Request){if(!await authenticated())return NextResponse.json({error:'Unauthorized'},{status:401});const origin=req.headers.get('origin');if(origin&&origin!==new URL(req.url).origin)return NextResponse.json({error:'Invalid origin'},{status:403});if(!process.env.MONGODB_URI)return NextResponse.json({error:'Connect MongoDB to save changes.'},{status:503});try{const b=await req.json();return NextResponse.json(await commitImport(b.token,b.associations||{},new MongoRepository()))}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Commit failed'},{status:400})}}
