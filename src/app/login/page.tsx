import {signIn} from './actions'; export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;return <main className="min-h-screen grid place-items-center p-4"><form action={signIn} className="card w-full max-w-sm space-y-4"><h1 className="text-2xl font-bold">Inventory access</h1><p className="text-slate-600">Enter the dashboard password.</p>{p.error&&<p role="alert" className="text-red-700">Unable to sign in. Check your password.</p>}<label className="block font-medium">Password<input required name="password" type="password" className="input mt-1" autoComplete="current-password"/></label><button className="btn w-full">Continue</button><p className="text-sm text-slate-600">Missing configuration? Run <code>npm run setup-auth</code>.</p></form></main>}
